<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bionime.mapper.UserMapper">
	<insert id="insert" parameterType="com.bionime.pojo.User">
		
	</insert>
	
	<select id="login" resultType="com.bionime.pojo.User" parameterType="com.bionime.utils.SystemResult">
		select * from user where name=#{name} and password=#{password}
	</select>
</mapper>